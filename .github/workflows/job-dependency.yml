name: Job Dependency

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "This is BUILD section"
        
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test
        run: echo "This is TEST section"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Deploy
        run: echo "This is DEPLOY section"
  
